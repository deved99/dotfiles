#!/bin/sh

run() {
    PROFILE=$1

    ansible-playbook \
      --extra-vars @vars/common.yml \
      --extra-vars @vars/$PROFILE.yml \
      playbook.yml -K
}

usage() {
    echo "USAGE:"
    echo "  $0 PROFILE"
    exit 1
}

[ -z ${1+x} ] && usage || run
