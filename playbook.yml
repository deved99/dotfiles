---
- name: Configuring {{ PROFILE }}
  hosts: localhost
  # tasks:
  #   - name: Test
  #     ansible.builtin.debug:
  #       msg: "{{ BASE16 }}"
  roles:
    - clone-repo
    - misc-packages
    - awesome # WM
    - menu # Well, I guess you can tell
    - kitty # Terminal
    - emacs # Editor
    - mpd # Music
